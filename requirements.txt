# GLM-OCR Production Server (RunPod branch) — Python dependencies

# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
gunicorn>=21.2.0
python-multipart>=0.0.9      # required for FastAPI file uploads

# --- Config ---
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# --- RunPod HTTP client ---
httpx>=0.27.0

# --- File conversion ---
pillow>=10.0.0
pypdfium2>=4.30.0             # PDF → images (no system poppler needed)
# LibreOffice (system package) is required for .doc/.docx → PDF conversion
